using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Arena : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//need to get gladiators from a gladiator pit
        //how often fights can happen is based off of a time variable which is impacted by number of employees
        //whenever a fight happens, give entertainment to nearby houses
        //this is a tier 1 entertainment building so it can only increase entertainment in nearby houses to tier 1
	}
}
